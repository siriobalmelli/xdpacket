# test_reflection.yaml
# incoming packets are simply reflected back out the same interface.
---
xdpk:
  - iface: enp0s3
  - field: any
    offt: 0
    len: 0
  - rule: reflect
    match:
      - any:
  - process: enp0s3
    rules:
      - reflect: enp0s3
...
